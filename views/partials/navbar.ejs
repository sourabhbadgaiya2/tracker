<div
  class="flex items-center justify-between text-zinc-300 text-lg px-8 py-4 border-b border-zinc-800"
>
  <div class="h-12 w-12 rounded-lg bg-zinc-500 overflow-hidden">
    <!-- <img src="" alt=""> -->
  </div>
  <div class="flex gap-4">
    <% if (user){ %>
    <h1 class="text-4xl cursor-pointer"><i class="ri-menu-4-fill menu"></i></h1>
    <div
      class="flex list flex-col absolute backdrop-blur-3xl backdrop-brightness-150 w-[20vw] text-2xl px-4 h-screen gap-6 py-4 top-0 -right-1/2"
    >
      <div class="text-4xl text-right">
        <i class="ri-close-circle-line cursor-pointer cross"></i>
      </div>
      <a
        class="hover:scale-125 pl-5 transition ease-in-out delay-0.3s"
        href="/users/profile"
        >Home</a
      >
      <a
        class="hover:scale-125 pl-5 transition ease-in-out delay-75"
        href="/about"
        >About</a
      >

      <!-- ------------------ -->

      <a
        class="hover:scale-125 pl-5 transition ease-in-out delay-75"
        href="/users/updateuser"
        >Edit profile</a
      >
      <a
        class="hover:scale-125 pl-5 transition ease-in-out delay-75"
        href="/users/changepassword"
        >Change password</a
      >
      <a
        class="hover:scale-125 pl-5 transition ease-in-out delay-75"
        href="/users/userdelete"
        >Delete Account</a
      >
      <a
        class="hover:scale-125 pl-5 transition ease-in-out delay-75"
        href="/users/logout"
        >Log out</a
      >
    </div>
    <!-- ---------------- -->
    <% } else{ %>
    <a href="/">Home</a> <a href="/about">About</a>
    <% } %>
  </div>
</div>

<script>
  const menu = document.querySelector('.menu');
  const cross = document.querySelector('.cross');
  const list = document.querySelector('.list');

  menu.addEventListener('click', () => {
    list.style.right = 0;
    list.style.transition = 'right 0.40s ease';
  });
  cross.addEventListener('click', () => {
    list.style.right = `${-100}%`;
    list.style.transition = 'right 2s ease';
  });
</script>
